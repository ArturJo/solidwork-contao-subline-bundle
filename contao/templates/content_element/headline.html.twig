{#
Extend the original core headline template.
The namespace @Contao refers to the core bundle templates.
#}
{% extends "@Contao/content_element/headline.html.twig" %}

{#
The core template usually defines a 'content' block
(inherited from _base.html.twig). We override it here.
#}
    {% block headline_component %}

        {# this.data.subline oben #}
        {% if this.data.subline is defined
            and this.data.subline is not empty
            and this.data.subline_position is defined
            and this.data.subline_position == 'above' %}
            <div class="sw-subline sw-subline--above">
                {{ this.data.subline|insert_tag|raw }}
            </div>
        {% endif %}

        {# Original Headline #}
        {{ parent() }}

        {# this.data.subline unten #}
        {% if this.data.subline is defined
            and this.data.subline is not empty
            and (this.data.subline_position is not defined or this.data.subline_position == 'below') %}
            <div class="sw-subline sw-subline--below">
                {{ this.data.subline|insert_tag|raw }}
            </div>
        {% endif %}

    {% endblock %}

